{
  "name": "angular-parity-fixtures",
  "version": "2.0.0",
  "description": "Parity fixture generator using Angular salary calculator models directly",
  "main": "src/generate-fixtures.ts",
  "scripts": {
    "generate": "bun run src/generate-fixtures.ts",
    "calculate": "bun run src/calculate.ts",
    "build": "tsc",
    "clean": "rimraf dist"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0",
    "rimraf": "^5.0.5"
  }
}
