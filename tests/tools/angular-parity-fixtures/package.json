{
  "name": "angular-parity-fixtures",
  "version": "2.0.0",
  "description": "Angular CLI for parity test calculations",
  "main": "src/calculate.ts",
  "scripts": {
    "calculate": "bun run src/calculate.ts",
    "build": "tsc",
    "clean": "rimraf dist"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0",
    "rimraf": "^5.0.5"
  }
}
