{
  "name": "angular-parity-fixtures",
  "version": "2.0.0",
  "description": "Parity fixture generator using Angular salary calculator models directly",
  "main": "src/generate-fixtures.ts",
  "scripts": {
    "generate": "ts-node --transpile-only src/generate-fixtures.ts",
    "calculate": "ts-node --transpile-only src/calculate.ts",
    "build": "tsc",
    "clean": "rimraf dist"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0",
    "ts-node": "^10.9.2",
    "rimraf": "^5.0.5"
  }
}
